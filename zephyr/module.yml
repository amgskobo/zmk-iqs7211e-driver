# Copyright (c) 2025-2026 amgskobo
# SPDX-License-Identifier: MIT

# Keep this identical to the west project and local module directory name.
name: zmk-iqs7211e-driver
build:
  # Folder containing a "CMakeLists.txt" (. = root)
  cmake: .
  # Kconfig filepath
  kconfig: Kconfig
  settings:
    # Folder containing a "dts" folder (. = root)
    dts_root: .
